
Do it once
python3.10.exe -m pip install virtualenv

Do it on first project open

./controllers/venv/bin/Activate.ps1
python3.10.exe -m pip install -r ./controllers/requirements.txt

Do it to run graph server
npm start